<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/logo/logo-recipinoy.png" type="image/icon type">

    <!-- font awesome cdn link  -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">

    <!-- custom css file link  -->
    <link rel="stylesheet" href="/public/css/style.css">

    <title>Profile</title>
</head>
<body>
    <!-- message -->
    <% if (msg != '') { %>
        <div class="message">
            <span> <%= msg %> </span>
            <i class="fas fa-times" onclick="this.parentElement.remove();"></i>
        </div>
    <% } %>   

    <!-- user header -->
    <%#- include('./partials/guestNav'); %>
    <%- include('./partials/userNav'); %>

<div class="user-details">
    <div class="user">
    <% user.forEach(u => { %>
        <form action="/profile" method="post" class="user-prof">
            <a href="/" class="back-btn"><span class="prof-back">Home</span></a>

            <!-- title -->
            <span class="titleheading">Profile</span>

            <div class="user-info">
                <p><i class="fas fa-user"></i> <span>Display Name: <input type="text" class="userText" name="userName" id="userName" value="<%= u.user_name %>" required></span></p>    
                <p><i class="fas fa-envelope"></i> <span>Email: <%= u.user_email %> </span></p>   
            </div>
            <br><br><br>

        <div class="flex-container">
            <div class="restrictions">
            <p><i class="fas fa-hamburger"></i> <span>Dietary Restrictions: </span></p>
            
            <% if (u.user_restrict){ %> 
                <% let rStr = u.user_restrict; %> 
                <% if (rStr.includes('lactose intolerance')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Lactose intolerance" checked>
                        <label for="dietRLacInt" class="rest-label">Lactose intolerance</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Lactose intolerance">
                        <label for="dietRLacInt" class="rest-label">Lactose intolerance</label>
                    </div>
                <% } %> 
                <% if (rStr.includes('gluten intolerance')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Gluten intolerance" checked>
                        <label for="dietRGutInt" class="rest-label">Gluten intolerance</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Gluten intolerance">
                        <label for="dietRGutInt" class="rest-label">Gluten intolerance</label>
                    </div>
                <% } %>
                <% if (rStr.includes('vegetarianism')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Vegetarianism" checked>
                        <label for="dietRVegetnism" class="rest-label">Vegetarianism</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Vegetarianism">
                        <label for="dietRVegetnism" class="rest-label">Vegetarianism</label>
                    </div>
                <% } %>
                <% if (rStr.includes('veganism')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Veganism" checked>
                        <label for="dietRVeganism" class="rest-label">Veganism</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Veganism">
                        <label for="dietRVeganism" class="rest-label">Veganism</label>
                    </div>
                <% } %>
                <% if (rStr.includes('kosher')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Kosher" checked>
                        <label for="dietRKosher" class="rest-label">Kosher</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Kosher">
                        <label for="dietRKosher" class="rest-label">Kosher</label>
                    </div>
                <% } %>
                <% if (rStr.includes('keto')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Keto" checked>
                        <label for="dietRKeto" class="rest-label">Keto</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Keto">
                        <label for="dietRKeto" class="rest-label">Keto</label>
                    </div>
                <% } %>
                <% if (rStr.includes('diabetes')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Diabetes" checked>
                        <label for="dietRDiab" class="rest-label">Diabetes</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Diabetes">
                        <label for="dietRDiab" class="rest-label">Diabetes</label>
                    </div>
                <% } %>
                <% if (rStr.includes('dairy-free')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Dairy-free" checked>
                        <label for="dietRDairy" class="rest-label">Dairy-free</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Dairy-free">
                        <label for="dietRDairy" class="rest-label">Dairy-free</label>
                    </div>
                <% } %>
                <% if (rStr.includes('low carb')) { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Low carb" checked>
                        <label for="dietRLowCarb" class="rest-label">Low carb</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="dietRInp" id="dietRInp" value="Low carb">
                        <label for="dietRLowCarb" class="rest-label">Low carb</label>
                    </div>
                <% } %>

            <% } else { %>
                <div class="structure">
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Lactose intolerance">
                    <label for="dietRLacInt" class="rest-label">Lactose intolerance</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Gluten intolerance">
                    <label for="dietRGutInt" class="rest-label">Gluten intolerance</label>
                </div>
                <div class="structure">    
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Vegetarianism">
                    <label for="dietRVegetnism" class="rest-label">Vegetarianism</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Veganism">
                    <label for="dietRVeganism" class="rest-label">Veganism</label>
                </div>
                <div class="structure"> 
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Kosher">
                    <label for="dietRKosher" class="rest-label">Kosher</label>
                </div>
                <div class="structure"> 
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Keto">
                    <label for="dietRKeto" class="rest-label">Keto</label>
                </div>
                <div class="structure"> 
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Diabetes">
                    <label for="dietRDiab" class="rest-label">Diabetes</label>
                </div>
                <div class="structure"> 
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Dairy-free">
                    <label for="dietRDairy" class="rest-label">Dairy-free</label>
                </div>
                <div class="structure"> 
                    <input type="checkbox" name="dietRInp" id="dietRInp" value="Low carb">
                    <label for="dietRLowCarb" class="rest-label">Low carb</label>
                </div>
            <% } %> 
            </div>

            <div class="allergies">
            <p><i class="fas fa-cheese"></i> <span>Food Allergies: </span></p>
            
            <% if (u.user_allergy) { %>
                <% let aStr = u.user_allergy; %> 
                <% if (aStr.includes("cow's milk")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="cow&rsquo;s milk" checked>
                        <label for="faCow" class="rest-label">Cow&rsquo;s milk</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="cow&rsquo;s milk">
                        <label for="faCow" class="rest-label">Cow&rsquo;s milk</label>
                    </div>
                <% } %>
                <% if (aStr.includes("eggs")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="eggs" checked>
                        <label for="faEggs" class="rest-label">Eggs</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="eggs">
                        <label for="faEggs" class="rest-label">Eggs</label>
                    </div>
                <% } %>
                <% if (aStr.includes("tree nuts")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="tree nuts" checked>
                        <label for="faTNuts" class="rest-label">Tree nuts</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="tree nuts">
                        <label for="faTNuts" class="rest-label">Tree nuts</label>
                    </div>
                <% } %>
                <% if (aStr.includes("peanuts")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="peanuts" checked>
                        <label for="faTPnuts" class="rest-label">Peanuts</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="peanuts">
                        <label for="faTPnuts" class="rest-label">Peanuts</label>
                    </div>
                <% } %>
                <% if (aStr.includes("shellfish")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="shellfish" checked>
                        <label for="faShllfish" class="rest-label">Shellfish</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="shellfish">
                        <label for="faShllfish" class="rest-label">Shellfish</label>
                    </div>
                <% } %>
                <% if (aStr.includes("wheat")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="wheat" checked>
                        <label for="faWheat" class="rest-label">Wheat</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="wheat">
                        <label for="faWheat" class="rest-label">Wheat</label>
                    </div>
                <% } %>
                <% if (aStr.includes("soy")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="soy" checked>
                        <label for="faSoy" class="rest-label">Soy</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="soy">
                        <label for="faSoy" class="rest-label">Soy</label>
                    </div>
                <% } %>
                <% if (aStr.includes("Fish")) { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="Fish" checked>
                        <label for="faFish" class="rest-label">Fish</label>
                    </div>
                <% } else { %>
                    <div class="structure">
                        <input type="checkbox" name="faInp" id="faInp" value="Fish">
                        <label for="faFish" class="rest-label">Fish</label>
                    </div>
                <% } %>
            <% } else { %>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="cow&rsquo;s milk">
                    <label for="faCow" class="rest-label">Cow&rsquo;s milk</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="eggs">
                    <label for="faEggs" class="rest-label">Eggs</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="tree nuts">
                    <label for="faTNuts" class="rest-label">Tree nuts</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="peanuts">
                    <label for="faTPnuts" class="rest-label">Peanuts</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="shellfish">
                    <label for="faShllfish" class="rest-label">Shellfish</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="wheat">
                    <label for="faWheat" class="rest-label">Wheat</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="soy">
                    <label for="faSoy" class="rest-label">Soy</label>
                </div>
                <div class="structure">
                    <input type="checkbox" name="faInp" id="faInp" value="Fish">
                    <label for="faFish" class="rest-label">Fish</label>
                </div>
            <% } %>
        </div>
    </div>
    <div class="profbuttons">
        <br><br><button type="submit" class="btn">save changes</button>
        </form>
        
    <% }) %>

    <button
      class="btn"  
      id="notify-me"
      onclick="notifyMe()"> Notify Me
    </button>
    </div>
    </div>
</div>


<script>
     /*// Bind function to onclick event for checkbox
document.getElementById('pushNotif').onclick = function() {
    // access properties using this keyword
    if ( this.checked ) {
        // Returns true if checked
        //alert( this.value );
        // (A) OBTAIN USER PERMISSION
        // (A1) ASK FOR PERMISSION
        if (Notification.permission === "default") {
            Notification.requestPermission().then((perm) => {
                if (Notification.permission === "granted") {
                regWorker().catch((err) => { console.error(err); } );
                }
                else { alert("Please allow notifications."); }
            });
        }
    } else {
        // Returns false if not checked
    }
};*/
    /*addEventListener('load', async () =>{
        let sw = await navigator.serviceWorker.register('./controllers/sw.js')
        console.log(sw)
    })

    async function notifyMe() {
        let sw = await navigator.serviceWorker.ready;
        let push = await sw.pushManager.notifyMe({
            userVisibleOnly: true,
            applicationServerKey: 'BAjsC077dFVN4--_-Tp87Vh-WJ8i7xjr1qpx3ZnmfB8uMgLU9nFjjwmVsUeBBDg5S-GWKuCwQQGydn3r5yJRC9k'
        })
        console.log(JSON.stringtify(push))
    }*/
let button = document.querySelector('button');
button.addEventListener('click', () =>{
    if(window.Notification) return;

    Notification
    .requestPermission()
    .then(console.log('notification on'))
});
</script>

    <!-- <div class="loader">
        <img src="/images/loader.gif" alt="">
    </div> -->

<!-- custom js file link  -->
<script src="/public/js/script.js"></script>
</body>
</html>